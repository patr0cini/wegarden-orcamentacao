services:
  - type: web
    name: wegarden-orcamentacao
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: APP_PASSWORD
        value: wegarden2026
      - key: SECRET_KEY
        generateValue: true
